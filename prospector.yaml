strictness: medium
test-warnings: true
doc-warnings: true

ignore-paths:
  - truelearn_experiments

pep8:
  full: true

pydocstyle:
  ignore:
    #Docstrings should start on the first line (i.e. the same one as the quotes)
    - D213
